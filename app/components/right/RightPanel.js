import React from 'react';
import AttributesPanel from './attributes_panel/AttributesPanel';

export default (props) => {
  return (
    <div id="right-panel">
      <AttributesPanel />
    </div>
  );
}
